/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com'], // Add any other external image domains here
  },
  // Optionally enable strict mode or other features
  reactStrictMode: true,
};

export default nextConfig;
